services:
  mosquitto-test:
    image: eclipse-mosquitto:2.0
    container_name: mqtt-test
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf
    restart: unless-stopped

networks:
  default:
    name: test-iot-network